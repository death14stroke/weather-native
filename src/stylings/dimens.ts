import { ms } from 'react-native-size-matters';

import { ds } from '@app/utils';

export const Dimens = {
	PADDING_SCREEN: ds(16),
	PADDING_CONTENT: ds(12),
	BORDER_RADIUS: ds(16),
	ITEM_SEPARATOR: ds(8),
	CARD_HOURLY_WEATHER: ms(112)
};
